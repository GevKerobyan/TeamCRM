import styled from "styled-components";

export const TaskListTopLine = styled.div`
position: fixed;
left: 0;
top: 65px;
padding: 0 100px;
height: 30px;
width: 100vw;
background: var(--white-over);
display: flex;
justify-content: space-between;
gap: 50px;
`;
